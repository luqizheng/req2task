# pnpm 10+ 配置
# 这些旧配置已被移除或改为默认值：
# - shamefully-hoist (默认 false)
# - auto-install-peers (默认 true)
# - strict-peer-dependencies
# - link-workspace-packages
# - shared-workspace-lockfile
# - save-workspace-protocol
